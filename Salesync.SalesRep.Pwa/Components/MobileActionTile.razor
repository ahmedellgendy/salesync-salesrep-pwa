<button type="button"
        class="mobile-action-tile @ColorClass @(Large ? "mobile-action-large" : "")"
        disabled="@Disabled"
        @onclick="OnClick">

    <div class="mobile-action-icon">
        @Icon
    </div>

    <div class="mobile-action-content">
        <div class="mobile-action-title">
            @Title
        </div>

        @if (!string.IsNullOrWhiteSpace(Subtitle))
        {
            <div class="mobile-action-subtitle">
                @Subtitle
            </div>
        }
    </div>

</button>

@code {
    [Parameter]
    public string Icon { get; set; } = "•";

    [Parameter]
    public string Title { get; set; } = string.Empty;

    [Parameter]
    public string? Subtitle { get; set; }

    [Parameter]
    public string ColorClass { get; set; } = "mobile-blue";

    [Parameter]
    public bool Large { get; set; }

    [Parameter]
    public bool Disabled { get; set; }

    [Parameter]
    public EventCallback OnClick { get; set; }
}